# Copy to .env.local for `vercel dev` (never commit .env.local)
GEMINI_API_KEY=
# Optional — richer related search in the browser flow
SERPER_API_KEY=
# Optional override (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash
